syntax = "proto3";

package simulet.proto.agent.v1;

import "simulet/journal/v1/journal.proto";

enum AgentType {
  UNKNOWN = 0;
  PERSON = 1;
  PRIVATE_CAR = 2;
  BUS = 3;
  TAXI = 4;
}

message Agent {
  uint64 id = 1;
  AgentType type = 2;
  repeated journal.v1.ODPair scheduls = 3;
}

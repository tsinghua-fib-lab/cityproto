syntax = "proto3";

package wolong.traffic.interaction.lane.v1;

import "wolong/traffic/interaction/agent/v2/agent.proto";

message State {
  // Lane ID
  int32 id = 1;
  // Lane上的人/车
  repeated wolong.traffic.interaction.agent.v2.AgentRuntime persons = 2;
  // 平均速度（m/s）
  double avg_v = 3;
  // 是否限行
  bool restriction = 4;
}

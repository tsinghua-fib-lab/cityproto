syntax = "proto3";

package wolong.sim.interaction.control.v1;

service ControlService {
  rpc Step(StepRequest) returns (StepResponse);
}

message StepRequest {
  uint32 step = 1;
}

message StepResponse {
}

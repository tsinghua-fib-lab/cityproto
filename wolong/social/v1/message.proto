syntax = "proto3";

package wolong.social.v1;

message Message {
  // 消息发送者ID
  int32 from = 1;
  // 消息接收者ID
  int32 to = 2;
  // 消息内容
  string message = 3;
  // 消息发出时间（秒），如无则为当前模拟时间
  // 对于接收到的消息，该字段总是存在
  optional int64 timestamp = 4;
}
syntax = "proto3";

package wolong.routing.v2;

message Cost {
    // 目标拓扑元素（只支持道路Road）
    int32 id = 1;
    // 路径成本（单位：秒）
    double cost = 2;
    // 设置的时间（单位：秒）
    // 即设置几点几分的道路通行成本为cost
    // 为空表示设置全天通行成本均为cost
    optional double time = 3;
}